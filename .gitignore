# OS artifacts
.DS_Store
Thumbs.db
.directory
.claude/

# Python caches, testing, and virtual environments
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
.pytest_cache/
.coverage
htmlcov/

# Machine Learning model caches & weights
.cache/
.huggingface/
hf_cache/
models/
*.onnx
*.pt
*.bin
*.safetensors

# Wallpaper download, buffer & temporary caches
incoming/
downloads/
cache/
temp/
tmp/
*.tmp
*.log

# Generated thumbnails cache
.thumbs/

# Database & SQLite files
*.db
*.sqlite
*.sqlite3
wallpapers.db
*.db-journal
*.db-wal
*.db-shm
.last_git_push

# Environment variables & secrets
.env
.env.local
.env.*.local
*.env
!.env.example

# Local Bulk Wallpaper Storage (Only manually curated wallpapers in Curated/ are published)
Wallpapers/

# Curated wallpapers are now served from CDN (cdn.skiddle.id/images/wallpapers/...)
# Local files kept on disk but no longer tracked in git to reduce repo size.
Curated/

# Web assets (generated by curate.py)
# Note: only generated files are ignored; keep web/ tracked if it contains hand-written source
# web/
